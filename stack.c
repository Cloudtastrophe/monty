#include "monty.h"

/**
 * f_stack - sets the stack to stack mode
 * @head: stack head
 * @counter: line_number
 *
 * Description:
 * Sets the stack to stack mode. In stack mode, elements are
 * added to the top of the stack.
 *
 * Return: No return value
 */
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
